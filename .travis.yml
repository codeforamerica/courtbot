language: node_js
services:
  - postgresql "9.4"
env:
- DATABASE_URL=postgres://localhost:5432/courtbot
before_script:
  - psql -c 'create database courtbot;' -U postgres
  - cp .env.sample .env
  - npm run dbsetup
